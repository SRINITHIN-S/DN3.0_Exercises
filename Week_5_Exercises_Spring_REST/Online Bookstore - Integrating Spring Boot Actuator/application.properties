# Expose all Actuator endpoints (for demonstration purposes)
management.endpoints.web.exposure.include=*

# Customize the base path for Actuator endpoints
management.endpoints.web.base-path=/actuator

# Enable sensitive details for health endpoint (optional, requires security consideration)
management.endpoint.health.show-details=always

# Enable custom info properties
management.info.env.enabled=true
